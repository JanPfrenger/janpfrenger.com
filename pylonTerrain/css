body { 
  background: #0f0f14; 
  overflow: hidden; 
}
canvas { 
  display: block; 
  cursor: crosshair; 
  touch-action: none; 
  position: absolute;
  top:0;
}
p {
  color: #EEE;
  position: relative;
  margin: .8em;
  z-index: 1;
  pointer-events:none;
}